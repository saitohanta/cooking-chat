<% if post.favorited_by?(current_member) %>
  <%= link_to post_favorites_path(post), method: :delete, remote:true, class:"btn btn-outline-secondary btn-sm" do %>
    <i class="fa-solid fa-thumbs-up"></i>️<%= post.favorites.count %>
  <% end %>
<% else %>
  <%= link_to post_favorites_path(post),method: :post, remote:true, class:"btn btn-outline-secondary btn-sm" do %>
    <i class="fa-regular fa-thumbs-up"></i><%= post.favorites.count %>
  <% end %>
<% end %>