<% post.comments.each do |comment| %>
  <%= image_tag comment.member.get_profile_image(50,50) %>
  <%= comment.member.name %>
  <%= comment.created_at.strftime("%Y/%m/%d") %><%= comment.content %>
  <%= link_to "削除", admin_post_comment_path(comment.post, comment), method: :delete, remote:true, data: {"turbolinks" => false} %>
<% end %>
